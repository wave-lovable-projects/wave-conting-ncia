import { PlaceholderPage } from '@/components/shared/PlaceholderPage';
import { Zap } from 'lucide-react';
export default function MetaDashboard() { return <PlaceholderPage title="Dashboard Meta API" icon={Zap} />; }
